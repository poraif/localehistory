{
  "name": "starter-template",
  "version": "1.0.0",
  "description": "A boilerplate code to quickly set up a new project with 'semantic-relase' installed as dev-dependency and configured to work with GitHub Actions in an already configured CI/CD pipeline",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Giacomo Cavarretta",
  "license": "ISC",
  "devDependencies": {
    "@semantic-release/changelog": "^6.0.3",
    "@semantic-release/commit-analyzer": "^11.1.0",
    "@semantic-release/git": "^10.0.1",
    "@semantic-release/github": "^9.2.6",
    "@semantic-release/release-notes-generator": "^12.1.0",
    "semantic-release": "^23.0.2"
  }
}
